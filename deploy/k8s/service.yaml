apiVersion: v1
kind: Service
metadata:
  name: careflow
  labels:
    app.kubernetes.io/name: careflow
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: careflow
  ports:
    - name: http
      port: 80
      targetPort: http
